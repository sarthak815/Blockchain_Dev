    # for x in mycol.find():
        #     self.new_index+=1
        #     print(x)
        #     blck = self.create_block(x['index'], x['timestamp'], x['sender'], x['receiver'],x['value'], x['previous_hash'] )
        #     self.chain.append(blck)
        #     #self.prev_hash = hash(blck)